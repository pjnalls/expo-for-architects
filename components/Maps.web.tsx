import { ThemedView } from './ThemedView';

export default function Maps() {
  return (
    <ThemedView className="flex-1">
      Mapsfor the web is currently being implemented.
    </ThemedView>
  );
}
